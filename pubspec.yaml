name: tapsi_client
description: Tapsi Injector App
publish_to: 'none'

# این بخش اضافه شد تا خطای SDK برطرف شود
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_inappwebview: ^6.0.0

flutter:
  uses-material-design: true
